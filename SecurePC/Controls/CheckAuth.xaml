<Page x:Class="SecurePC.Controls.CheckAuth"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="360" d:DesignWidth="710" MinHeight="360" Width="710" FontSize="14" Title="Check Auth">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" DockPanel.Dock="Bottom" OpacityMask="{x:Null}">
            <ProgressBar Name="_progressBar"  Height="10" MaxWidth="300" Width="100" IsIndeterminate="True"/>
        </StackPanel>

    </Grid>
</Page>
